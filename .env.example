# Infisical project IDs
INFISICAL_SHARED_PROJECT_ID=
INFISICAL_DISCOVERY_PROJECT_ID=

# Judge LLM (AGNES_AI_API_KEY loaded via Infisical shared project at runtime)
# AGNES_AI_API_KEY set by infisical export — never committed.

# Web search backends (all OPTIONAL):
# BRAVE_API_KEY — Brave Search API ($5 free credits / month). Higher quality.
# If absent, DuckDuckGo (no key) is used automatically.
# DISABLE_WEB_SEARCH=1 — forces NoopSearcher (offline mode).
